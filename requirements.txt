# MCP Server
mcp>=1.0.0

# PostgreSQL async client (replaces httpx — connects directly to PrintSmith DB)
asyncpg>=0.29.0

# HTTP server for remote transport (LXC/Docker)
uvicorn>=0.30.0
starlette>=0.38.0

# Async support
anyio>=4.0.0
